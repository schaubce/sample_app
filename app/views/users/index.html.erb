<% provide(:title, 'All users') %>
<h1> All users </h1>

<%= will_paginate %>

<ul class = "users">
  <% @users.each do |user| %>
    <li>
    <%= gravatar_for user, options = {:size=>52}  %>
      <%= render user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
